# Copy this to .env if you need optional local API keys. Never commit .env.

# Optional, off by default — unofficial MetaTFT comp tier-list fetch (scripts/fetch-metatft.js).
# See that script's header comment for why this is isolated and not wired into the app.
# ENABLE_METATFT_FETCH=true
# PARSE_API_KEY=...
